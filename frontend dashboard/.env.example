# Open Ledger Frontend Environment Variables
# If running backend on a different port/host (e.g. Express on 5000, FastAPI on 8000), set it here.
# Leave empty to use Vite's default /api proxy (configured in vite.config.js).
VITE_API_BASE_URL=http://localhost:5000/api
