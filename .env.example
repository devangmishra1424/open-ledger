# Supabase Postgres — use the POOLED (port 6543, "Transaction" mode) connection string when
# deployed to a serverless host (Vercel etc.); Project Settings > Database > Connection pooling.
DATABASE_URL=
OPENAI_API_KEY=
TENSORMUX_API_KEY=
TENSORMUX_BASE_URL=https://api.tensormux.com/v1
TENSORMUX_MODEL=glm-4-7-flash
SMALLEST_API_KEY=
DODO_PAYMENTS_API_KEY=
